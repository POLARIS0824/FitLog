# 2026-08-07

## 提交前全面代码审查（38 文件，+836/−205）
- 变更分四组：防备份恢复崩溃（backup rules 排除密文文件 + KeystoreManager.decryptOrNull + mapper/converter 枚举降级）、AI 教练对话页完善（顶栏返回 + StackedSnackbar + Today 页入口 + ChatKey 路由）、TrainingLevelCalculator 只统计 WORKING 正式组、测试基建修复（createTestPreferencesDataStore 需传入共享 TestCoroutineScheduler 的 scope）。
- testDebugUnitTest BUILD SUCCESSFUL，全部测试通过。
- 给用户的建议：.idea/vcs.xml 被 IDE 改坏建议剔除；hs_err_pid1192.log 历史上被跟踪进仓库，建议 git rm --cached + gitignore。
- 注意：本机 Git Bash 无 cygpath，./gradlew 无法运行，须用 PowerShell 调 gradlew.bat。
